<div class="container">
  <p style="color: green"><%= notice %></p>

<h1>Stores</h1>

<div id="stores">
  <% @stores.each do |store| %>
    <%= render store %>
    <p>
      <%= link_to "Show this product", store ,class:"btn btn-outline-primary"%>
    </p>
  <% end %>
</div>

</div>